<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="proiectMaui.WorkoutDetailsPage"
             Title="Detalii antrenament">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="10">

            <Label x:Name="titleLabel" FontSize="24" FontAttributes="Bold" />
            <Label x:Name="typeLabel" FontSize="18" TextColor="DarkGray" />
            <Label x:Name="dateLabel" FontSize="16" />
            <Label x:Name="durationLabel" FontSize="16" />

            <Label Text="Note:" FontAttributes="Bold" IsVisible="False" />
            <Label x:Name="notesLabel" LineBreakMode="WordWrap" />

            <BoxView Color="LightGray" HeightRequest="1" />

            <Label Text="Exerciții:" FontAttributes="Bold" IsVisible="False" />
            <CollectionView x:Name="exerciseList">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Label Text="{Binding ReprDisplay}" />
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
